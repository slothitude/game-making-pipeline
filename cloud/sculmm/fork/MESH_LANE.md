# MESH_LANE — character mesh decision (OPEN)

1. Hunyuan3D 2.1 local attempt in flight (user-gated)
2. Meshy API fallback (cloud, auto-rig, ~$0.20/character)
3. Rejected: Hunyuan 2.0-flat (bas-relief), TRELLIS-512 (blocky cube)

> To be filled in after the user's verdict. Do not start a lane without
> that verdict — the user gates every look.
